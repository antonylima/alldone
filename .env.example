# Supabase Configuration
# Get these values from your Supabase project settings at https://supabase.com/dashboard

# Your Supabase project URL
VITE_SUPABASE_URL=your_supabase_project_url

# Your Supabase anonymous key (public key)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: For password recovery to work via email, you need to:
# 1. Configure email settings in Supabase Dashboard > Authentication > Email Templates
# 2. Set up a custom SMTP provider or use Supabase's built-in email service
# 3. Customize the password recovery email template if needed
# 4. Ensure the redirect URL (set in the app) matches your domain
